extends layout
block content
    - console.log(data)
    .main-content 
    h1 All Admin 
    table(border=1)
        for item in data 
            tr  
                td #{item.home_id}
                td #{item.home_name}
                td #{item.full_address}
                td #{item.admin_name}
